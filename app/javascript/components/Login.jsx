import React from 'react';
import PropTypes from 'prop-types';

const Login = () => (
  <div>
    <ons-button modifier="material">Login</ons-button>
    <ons-button modifier="large">Sign Up</ons-button>
  </div>
);

export default Login;
